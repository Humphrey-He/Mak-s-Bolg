---
slug: swe-agent-codeact-deep-reading
title: SWE-agent 与 CodeAct 精读：我怎么理解 coding agent 真正难的地方
description: 我把 SWE-agent 和 CodeAct 放在一起读，不是为了做论文对比表，而是为了弄清楚 coding agent 到底难在模型、难在工具，还是难在 agent-computer interface 与可执行动作空间。
date: '2026-06-08'
tag: AI Agent
readTime: 24 min
top: false
featured: true
series: AI Agent
seriesSlug: ai-agent
seriesOrder: 999
---

前面我已经单独写了一篇 Claude Code 的技术拆解，但那篇文章更多是在看“一个 coding agent 产品形态是怎么长出来的”。

如果再往下追，我其实一直想回答一个更底层的问题：

> coding agent 到底真正难在哪里？

是模型不够强？
是代码理解不够深？
是工具不够多？
还是说，问题根本不在这些表层，而在 agent 怎样进入计算机环境、怎样行动、怎样拿反馈、怎样把动作空间组织对？

我后来发现，把 `SWE-agent` 和 `CodeAct` 放在一起读，这个问题会特别清楚。

因为这两篇论文虽然切口不同，但都在推动同一件事：

- `SWE-agent` 在强调 `agent-computer interface`
- `CodeAct` 在强调 `executable actions`

如果把它们放在同一张图里，我会看到一条很清晰的主线：

```text
coding agent 不是靠“多想几步”成立的
而是靠“有用的动作空间 + 可执行反馈闭环”成立的
```

这也是我写这篇精读的动机。我不想把它们写成两篇论文摘要的拼盘，而是想借它们把 coding agent 的第一性问题拆出来。

---

## 一、我先说结论：这两篇论文都在把注意力从“模型会不会”移到“系统怎么让模型做”

如果整篇文章只允许我带走一句话，那我会带走这个：

> **coding agent 的上限，不只由模型能力决定，更由动作接口和反馈闭环决定。**

我特别喜欢这条主线，因为它一下子就把很多模糊讨论收紧了。

以前大家谈代码智能体，很容易掉进一种默认视角：

```text
模型更强
→ 代码能力更强
→ agent 更强
```

但 `SWE-agent` 和 `CodeAct` 其实都在提醒我：

```text
模型再强
如果不会有效行动
不会拿反馈
不会在环境里迭代
那它也很难成为真正可靠的 coding agent
```

这一点对我非常重要。因为它和我自己做系统时越来越强的感觉完全一致：很多失败，并不是因为模型不会写，而是因为系统没有把“怎么找、怎么改、怎么跑、怎么继续”这条链搭对。

---

## 二、为什么我觉得 SWE-bench 是这条线的背景而不是主角

在正式说 `SWE-agent` 和 `CodeAct` 之前，我会先把 `SWE-bench` 放在脑子后面当背景。

原因很简单：它给 coding agent 提供了一个很重要的真实任务语境。

`SWE-bench` 关心的不是：

- 你能不能补全一段函数
- 你能不能写出 LeetCode 答案

而是：

- 你能不能在真实仓库里修 issue
- 你能不能面对已有代码结构
- 你能不能根据反馈持续推进修复

这件事特别关键。因为它把问题从“代码生成”变成了“软件工程任务执行”。

我之所以先提这个背景，是因为 `SWE-agent` 和 `CodeAct` 都不是在真空中说话。它们都在回应这样一个现实：

> 只会生成代码片段，不足以解决真实软件工程问题。

---

## 三、我怎么理解 SWE-agent：它最值钱的不是 agent，而是 ACI

如果只看名字，`SWE-agent` 很容易让人以为它的重点是“软件工程 agent 实现方案”。

但我自己读下来，最打动我的并不是“它用了 agent loop”这件事，而是它把问题讲得非常透：

> 自动化软件工程的关键，不只是模型，而是 `agent-computer interface`。

我会把这句话当成整篇论文最值钱的中心句。

### 3.1 什么叫 agent-computer interface

在我的理解里，ACI 说白了就是：

> 你到底给模型一个什么样的电脑世界，让它能有效行动。

这件事看起来像实现细节，但其实是核心设计。

如果接口很差，模型会遇到这些问题：

- 看不到合适的文件
- 搜不到关键位置
- 动作粒度太粗或太细
- 反馈噪声太大
- 无法把命令输出转成下一步策略

所以 ACI 不是锦上添花，而是 agent 是否能工作起来的地基。

### 3.2 我为什么特别认同这条路

因为它和我自己的工程经验太一致了。

很多人会本能地把失败归咎于“模型还不够聪明”，但我现在越来越愿意先问：

- 它的动作空间清不清楚；
- 它能不能有效找到相关文件；
- 命令返回是不是对模型友好；
- 编辑行为是不是足够稳定；
- 环境反馈能不能被继续利用。

这些问题，本质上都是 ACI 问题。

也正因为这样，`SWE-agent` 让我特别有共鸣。它没有把希望全压在“更大模型”上，而是在强调：**给模型一个更合适的操作表面，本身就是核心创新点。**

---

## 四、我怎么理解 CodeAct：它最值钱的不是 code，而是 action

如果说 `SWE-agent` 是从 interface 角度把问题挑明了，那 `CodeAct` 对我最大的价值，就是它把“动作”这件事推到了最前面。

我自己给 `CodeAct` 的一句话总结会是：

> 不要只让模型说它想做什么，而要让它用可执行代码把动作真正做出来。

这件事为什么重要？因为一旦动作可执行，系统就从“语言描述任务”跃迁到了“在环境里实施任务”。

### 4.1 为什么 executable actions 让我特别在意

很多 agent 在表面上已经会“规划”和“反思”了，但如果行动层还是很弱，整个系统很容易卡在下面这种状态：

```text
它知道自己应该查什么
但查不到

它知道自己应该改哪里
但改不稳

它知道自己应该验证
但验证链条不完整
```

`CodeAct` 这类工作给我的启发是：一旦行动以可执行代码为载体，模型就能把很多原本停留在自然语言里的意图，变成真正落地的操作。

### 4.2 我为什么不把它只看成“会写脚本”

如果只从表面理解，很容易把 `CodeAct` 误读成：

```text
让模型多输出一点可执行代码
```

但我觉得它真正推进的是另一层：

> 用统一的可执行动作空间，把计划、工具调用和环境交互绑在一起。

这个统一动作空间对 agent 很重要。因为一旦动作表示更统一，模型就更容易在不同任务之间迁移策略，而不是每次都依赖零散工具描述。

从这个角度看，`CodeAct` 真正关心的是行为载体，而不只是代码文本。

---

## 五、把两篇放在一起看，我会得到一条很清晰的主线

我自己最喜欢的读法，不是把 `SWE-agent` 和 `CodeAct` 分开背结论，而是把它们放在一起看成 coding agent 两个互补方向。

### 5.1 SWE-agent 在回答：怎样把计算机环境暴露给 agent

它最关心的是：

- 工具面怎么设计
- 交互界面怎么组织
- 搜索、编辑、执行这些能力怎样变得可用
- 环境反馈怎样帮助后续决策

它是从“界面和环境”这边切进去的。

### 5.2 CodeAct 在回答：agent 的动作应该如何表示和执行

它更关心的是：

- 动作如何统一表达
- 计划怎么变成真正可执行的步骤
- 为什么 code as action 能提升环境交互能力

它是从“动作空间”这边切进去的。

### 5.3 它们共同在回答：coding agent 为什么不能只做文本生成

这才是我最看重的地方。

这两篇论文虽然技术形式不同，但都在逼我承认一件事：

> coding agent 的问题，从来都不是只靠下一段代码生成就能解决的。

它真正需要的是：

- 有用的动作表面
- 可执行的动作载体
- 高质量环境反馈
- 多步任务状态维持

而这些东西一旦缺任何一个，系统就会从 agent 退化回“会说但不会真干”的代码助手。

---

## 六、这两篇论文怎样改变我对 coding agent 的评价标准

如果没有读这两篇论文，我很容易用下面这些标准去看 coding agent：

- 写代码快不快
- 补全像不像人
- 解释准不准
- 单次回答有没有惊艳感

但读完之后，我的评价标准明显变了。

### 6.1 我会先看动作空间是不是对的

也就是：

- 它能做哪些动作；
- 动作边界清不清楚；
- 能不能覆盖真实软件工程任务的关键链路。

### 6.2 我会看环境反馈是不是足够可用

比如：

- shell 输出是否可诊断
- 测试反馈是否能驱动下一步
- 搜索结果是否足够帮助定位
- 错误信息是否会被噪声淹没

### 6.3 我会看 agent 是否真的能在任务里持续推进

这比“单次写得多漂亮”更重要。

我更关心：

- 它会不会卡死
- 会不会反复兜圈
- 能不能基于反馈收敛
- 有没有明显的 stopping / correction 机制

### 6.4 我会看 interface 和 action 的设计是不是配套

这也是我从这两篇论文里得到的一个非常明确的收获：

> 只有界面设计好、不够；只有动作表达统一、也不够。两者必须一起成立。

如果 ACI 很差，再好的动作策略也落不了地；如果动作空间很弱，再好的界面也只是好看。

---

## 七、我为什么觉得这两篇论文和 Claude Code 能自然接上

前面那篇 Claude Code 技术拆解里，我已经写过一些判断：

- coding agent 的核心不是对话，而是环境闭环
- 上限强依赖 ACI
- 工具和反馈设计比总 prompt 更值钱

我现在回头看，会发现这些判断其实都能在 `SWE-agent` 和 `CodeAct` 里找到更坚实的论文支撑。

### 7.1 Claude Code 更像是 ACI 和 executable actions 的产品化表面

如果用一句话说，我会这么概括：

> `SWE-agent` 和 `CodeAct` 在论文里讨论的是 coding agent 该怎么工作；Claude Code 则像是在产品里回答“那它怎么被开发者真正用起来”。 

这三者不是竞争关系，而是谱系关系。

### 7.2 它们共同说明：真正的竞争不只在模型，而在 runtime

我现在越来越愿意相信，coding agent 的竞争最后未必只是模型分数竞争，更可能是：

- runtime 设计
- interface 设计
- action design
- feedback loop design
- permission boundary design

而这恰好就是 `SWE-agent`、`CodeAct` 和 Claude Code 三者共同照亮的地方。

---

## 八、我把这两篇论文带回自己系统里，会留下哪些方法论

如果不复述论文细节，只说我真正会带回工程里的东西，我会留下这几条。

### 8.1 先设计 ACI，再谈更强 agent

这几乎是我最认同的一条。

如果文件读写、仓库搜索、命令执行、反馈表示都还很粗糙，我不会急着往上堆更复杂的规划层。

### 8.2 把动作空间设计成系统核心，而不是实现细节

动作空间不只是“有哪些工具”，还包括：

- 粒度
- 表示方式
- 稳定性
- 是否利于复用

这会直接决定 agent 是否能真的持续推进任务。

### 8.3 Ground truth 永远比空转推理更值钱

代码任务里最值钱的反馈往往来自：

- 测试结果
- 编译结果
- 执行报错
- 搜索命中

这也是为什么我现在会更早给系统设计“反馈点”，而不是一味延长思维链。

### 8.4 评价 coding agent，重点是任务推进能力

我不会再太迷信“这次回答写得真漂亮”，而会更关注：

- 有没有推进任务
- 推进得稳不稳
- 成本和步骤是否可接受
- 出错后能不能恢复

---

## 九、结语：我为什么觉得这两篇论文值得放在一起读

对我来说，`SWE-agent` 和 `CodeAct` 放在一起读，最大的好处不是更方便做论文对照，而是它们能一起帮我把一个很容易被说虚的问题压实：

> coding agent 真正难的地方，不只是生成代码，而是怎样让模型进入一个可行动、可反馈、可持续推进的软件工程环境。

如果只留一句我会长期带着走的话，那大概就是：

> **coding agent 的核心，不是更会说代码，而是更会在电脑里做事。**

`SWE-agent` 让我更清楚地看到 interface 的重要性，`CodeAct` 让我更清楚地看到 action 的重要性。两者合起来，几乎就把 coding agent 的地基讲透了。

这也是为什么我觉得它们值得单独精读。因为它们真正给我的，不是两套可以直接照抄的实现，而是两条非常稳的判断：

- agent 要有合适的电脑界面
- agent 要有真正可执行的动作空间

只要这两条没想清楚，coding agent 很容易一直停留在“看起来很聪明，但很难稳定把任务做完”的阶段。

---

## 参考

1. [SWE-agent: Agent-Computer Interfaces Enable Automated Software Engineering](https://papers.neurips.cc/paper_files/paper/2024/file/5a7c947568c1b1328ccc5230172e1e7c-Paper-Conference.pdf)
2. [Executable Code Actions Elicit Better LLM Agents](https://arxiv.org/abs/2402.01030)
3. [SWE-bench: Can Language Models Resolve Real-World GitHub Issues?](https://arxiv.org/abs/2310.06770)
4. [RepoCoder: Repository-Level Code Completion Through Iterative Retrieval and Generation](https://arxiv.org/abs/2303.12570)
